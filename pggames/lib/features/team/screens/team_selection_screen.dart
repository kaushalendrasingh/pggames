import 'package:flutter/material.dart';

class TeamSelectionScreen extends StatelessWidget {
  const TeamSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement team selection UI and logic
    return Scaffold(
      appBar: AppBar(title: const Text('Team Selection')),
      body: const Center(child: Text('Team Selection Screen')),
    );
  }
}
